version: "3"

services:
  app:
    build: .
    ports:
      - "5000:5000"

    links:
      - db

  db:
   image: mysql:5.6
   ports:
    - "3306:3306"

   environment:
    MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    MYSQL_DATABASE: lostandfound

